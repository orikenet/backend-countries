1. Install:

   helm upgrade --install backend2 . \
     --namespace skycast --create-namespace \
     --values values-<env>.yaml

2. Verify:

   kubectl get pods -l app.kubernetes.io/instance=backend2
   kubectl get svc -l app.kubernetes.io/instance=backend2
